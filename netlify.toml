[build]
  command = "pnpm install --frozen-lockfile && pnpm --filter @workspace/learn run build"
  publish = "artifacts/learn/dist/public"

[build.environment]
  NODE_VERSION = "22"
  PNPM_VERSION = "10"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
